{
  "devDependencies": {
    "typescript": "^3.5.3"
  },
  "dependencies": {
    "axios": "^0.21.1",
    "node-axios": "^0.1.0"
  },
  "scripts": {
    "start:db": "json-server -w db.json",
    "start:parcel": "parcel index.html",
    "start": "npm run start:*"
  }
}
